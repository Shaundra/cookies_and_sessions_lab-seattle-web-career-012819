<h1>Your Cart</h1>

<ul>
  <% cart.each do |item| %>
    <li>
      <%= item %>
    </li>
  <% end %>
</ul>

<h2>Add an Item</h2>
<%= form_tag(controller: 'products', action: 'add') do %>
  <%= text_field_tag 'product' %>
  <%= submit_tag 'add to cart' %>
<% end %>
